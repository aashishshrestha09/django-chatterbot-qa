"""
Django Models for Chat Application

This module defines database models for the chat application.
Currently, ChatterBot manages its own database, so no custom models are needed.
"""

# from django.db import models

# No custom models needed - ChatterBot handles its own data storage
# This file exists to satisfy Django app structure requirements
